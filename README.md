# My Emacs .files

As of `2026/09/20`, my dotfiles for emacs support exclusively Rust, since that's the only language server package I've included in `init.el`.

This uses the [Spacemacs](https://github.com/syl20bnr/spacemacs) theme.
